// import { useState } from 'react';
// import SearchBar from '../../components/searchbar/SearchBar';
import * as styled from './Page2.style';

export default function Page2() {
  // const [searchKeyword, setSearchKeyword] = useState<string>(''); // 검색어 상태

  // const handleSearch = (keyword: string) => {
  //   console.log('검색어:', keyword); // 검색어 확인 (테스트용)
  //   setSearchKeyword(keyword); // 검색어 업데이트
  // };

  return (
    <styled.Page2Wrapper>
      {/* <SearchBar searchValue={handleSearch} /> 검색어 업데이트 함수 전달 */}
    </styled.Page2Wrapper>
  );
}
